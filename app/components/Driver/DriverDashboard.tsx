'use client';

export default function DriverDashboard() {
  return (
    <div>
      <p>This is the driver-specific UI.</p>
      {/* You can add map, incoming ride requests, etc. here */}
    </div>
  );
}
